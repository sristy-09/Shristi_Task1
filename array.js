let a = ["Movie 1:Inception","Movie 2:Interstellar","Movie 3:The Dark Knight"]

for (let index = 0; index < a.length; index++) {
        const element = a[index];
        console.log(element)
}

// a.length is the length of array which is 3 
// while index is the numbering of array which starts from 0 to 2
// a[index] returns the value in array relevant to its index i.e. a[0] = Movie 1:Inception